export type categoryProps ={
        id: string,
        title: string,
        slug: string,
        image: string,
        description: string,
        createdAt: string,
        updatedAt: string
}